# 任务拆分：垂直切片、小 PR、依赖、检查点和回滚点

导航：上一篇 [[07-Plan Before Code]] · [[VibeCoding课程首页]] · [[VibeCoding课程总纲]] · 下一篇 [[09-上下文工程]]

> [!summary] 核心问题
> 一个大 Plan 如果直接交给 Agent，错误会扩散到很多层，审查和回滚都很昂贵。好的任务拆分按“可观察行为”形成垂直切片，让每一步都能独立证明、独立审查、必要时独立撤销。


## 按可观察能力切，不按技术层堆半成品

“先写 Controller、再写 Service、再写 Repository”让每一步都无法独立证明业务价值。垂直切片应从一个验收场景穿过必要层，并在结束时有测试证据。

一个合格小任务应具备：

- 单一、清晰的验收结果。
- 可列举的文件范围和所有者。
- 已知输入依赖和对后续任务的输出。
- 目标测试、检查点和失败停止条件。
- 能单独撤销或恢复的边界。

小 PR 不是越小越好。只改一个 DTO 却让仓库无法编译，不是可审阅切片；把状态、退款、审计、迁移和所有测试塞进一个 PR，也不是。

## 四个垂直切片

| 切片 | 可交付行为 | 主要范围 | 验证 |
|---|---|---|---|
| 1 | CREATED 订单经 API 取消为 CANCELED | Domain、UseCase、Controller | 领域测试 + MockMvc |
| 2 | PAID 订单写入 CANCELING 与唯一退款任务 | Domain、Outbox、事务服务 | 应用集成测试 |
| 3 | 退款 Worker 成功/失败可重试且幂等 | Handler、RefundGateway | 调用次数 + 状态测试 |
| 4 | 并发、审计、完整错误契约 | 锁、审计、异常映射 | 并发测试 + 契约测试 |

依赖关系是 `1 → 2 → 3`，切片 4 的审计字段可与 3 并行研究，但修改同一领域对象时仍应串行合并。

## 检查点与回滚点

```text
检查点 A：切片 1 目标测试全绿，Diff 只含取消 CREATED 所需范围。
回滚点 A：可独立撤销切片 1，不影响现有订单查询。

检查点 B：切片 2 的订单状态与 outbox 同事务；重复 requestId 唯一。
停止条件 B：需要新增表但迁移策略未批准。

检查点 C：Fake RefundGateway 证明成功、失败和重复消费。
回滚点 C：禁用 Worker 消费，不删除已记录的取消意图。
```

## 可以怎样做

1. 把 Spec 场景按业务价值排序：最简单成功路径→外部副作用→失败/并发。
2. 为每个场景切出最少跨层文件。
3. 标出共享热点文件，避免两个任务同时改同一符号。
4. 为每个任务写“进入条件、完成证据、停止条件”。
5. 规划合并顺序和可恢复操作。
6. 每个切片结束都审查 Diff，再启动下一个。

## 动手梳理

把“实现订单取消全部功能”拆成 3～5 张任务卡。每张必须写：

```markdown
目标行为：
文件所有权：
依赖：
目标测试：
检查点：
停止条件：
回滚方式：
```

然后检查：是否存在没有用户可观察结果的纯技术任务？若存在，把它合并进需要该基础设施的切片。

## 自查清单

- [ ] 每个任务都有独立可验证行为。
- [ ] 依赖和合并顺序明确。
- [ ] 共享文件不会被并行任务同时修改。
- [ ] 每个任务都有检查点、停止条件和回滚方式。
- [ ] 没有产生长期不可运行的中间状态。

## 实战速查卡

### 什么时候查这篇文章

- 任务要同时修改很多层。
- 多个 Agent 争用同一文件。
- 中间状态长期无法运行。
- PR 太大而无法有效评审。

### 先做什么

- 按业务场景排序。
- 为每个场景切出最少跨层文件。
- 标出共享热点。
- 写进入条件、完成证据和停止条件。
- 安排合并与回滚顺序。

### 可直接借鉴的方法

每张任务卡包含：目标行为、文件所有权、依赖、目标测试、检查点、停止条件、回滚方式。

### 常见误区

- 按 Controller/Service/Repository 横向拆。
- 追求单文件小任务却无法编译。
- 多个任务同时改同一符号。
- 没有检查点就继续下一片。

### 完成证据

- 3～5 个可独立验证的垂直切片。
- 依赖图。
- 共享文件所有权。
- 每片的测试、停止与回滚方案。

### 延伸阅读

[[07-Plan Before Code]] · [[15-Git安全工作流]] · [[16-多Agent并行]]

## 结语

合理拆分让 Agent 的错误局部化，也让审查、并行和恢复更便宜。延伸阅读：[[09-上下文工程]]。

---

相关项目文档：[[实施任务模板]]
