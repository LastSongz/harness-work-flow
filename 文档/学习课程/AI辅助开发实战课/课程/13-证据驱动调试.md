# 证据驱动调试：复现、假设、插桩、最小修复、回归测试

导航：上一篇 [[12-核心执行循环]] · [[VibeCoding课程首页]] · [[VibeCoding课程总纲]] · 下一篇 [[14-AI代码评审]]

> [!summary] 核心问题
> “可能是并发问题”不是调试结论。真正可靠的调试从稳定复现开始，用证据筛选假设，通过插桩或最小实验定位原因，最后用回归测试锁住修复。


## 先稳定复现，再讨论修复

证据驱动调试采用漏斗：

1. **复现**：记录版本、输入、命令、频率、预期和实际。
2. **定位**：沿数据流找第一次偏离预期的位置。
3. **假设**：提出少量互斥、可证伪的根因。
4. **实验/插桩**：一次只改变一个变量，收集日志、调用次数、线程或数据库证据。
5. **最小修复**：修根因，不压制错误消息或扩大重构。
6. **回归**：新增先红后绿测试，并运行相邻场景。

不要一看到 `NullPointerException` 就到处加空判断；不要因为重试后通过就把问题归因于网络。Agent 适合生成候选假设和查相似路径，人要确保实验能区分这些假设。

## 并发取消导致两次退款

**复现**

```text
给同一 PAID 订单并发发送 requestId=R-10 与 R-11。
20 次运行中有 6 次 RefundGateway 调用两次。
两个请求都在日志中读取到状态 PAID。
```

**候选假设**

| 假设 | 可区分证据 |
|---|---|
| H1 领域规则允许重复取消 | 单线程连续调用也会产生两个决策 |
| H2 乐观锁未生效 | 两个事务都成功提交相同旧版本 |
| H3 outbox 缺少业务唯一键 | 订单只更新一次，但表中有两条退款任务 |
| H4 消费者重复投递 | outbox 只有一条，Handler 调用两次 |

先查询事务提交、订单版本和 outbox 数量。若证据显示一次订单更新、两条 outbox，根因更接近 H3，而不是去修改退款客户端重试。

**最小修复**

- 用 `(order_id, cancellation_request_group)` 或项目认可的业务键建立唯一性。
- 应用层把唯一冲突映射为读取原取消结果。
- `RefundGateway` 继续使用稳定幂等键，形成第二道保护。

**回归测试**

```java
@Test
void concurrent_cancel_requests_create_one_refund_job() throws Exception {
    runConcurrently(
        () -> cancel("O-200", "R-10"),
        () -> cancel("O-200", "R-11"));

    assertThat(refundOutbox.countForOrder("O-200")).isEqualTo(1);
}
```

测试应在修复前稳定暴露问题；若竞争窗口太小，可用屏障控制两个线程同时读取旧状态，而不是靠 `sleep` 猜时序。

## 可以怎样做

1. 保存可复制的复现命令和失败频率。
2. 对比成功与失败运行，找第一个差异。
3. 写 2～4 个互斥假设和各自证据。
4. 选择信息增益最高、侵入最小的实验。
5. 只在定位后修改根因。
6. 写回归测试并确认修复前红、修复后绿。
7. 检查相似调用路径是否存在同类问题。

## 动手梳理

假设 outbox 只有一条，但退款供应商收到两次调用。写三条新假设，并为每条指定证据。至少覆盖消息重复投递、Handler 幂等记录和供应商响应超时后的客户端重试。

再写停止条件：如果只能访问含真实支付信息的生产日志，应先获得授权和脱敏方案，不能直接复制给 Agent。

## 自查清单

- [ ] 复现包含版本、输入、命令、预期、实际和频率。
- [ ] 假设可证伪，实验能区分至少两个假设。
- [ ] 修复针对根因，没有顺手大重构。
- [ ] 回归测试在修复前能失败、修复后能通过。
- [ ] 已检查同类路径与数据安全边界。

## 实战速查卡

### 什么时候查这篇文章

- Bug 只能偶尔出现。
- Agent 一次提出很多猜测并同时修改。
- 日志很多却无法证明因果。
- 修复后不知道是否会复发。

### 先做什么

- 记录最小复现条件。
- 保留原始错误和环境。
- 一次只验证一个假设。
- 在关键边界增加可撤销观测。
- 做最小修复并补回归测试。

### 可直接借鉴的方法

复现 → 收集事实 → 排序假设 → 设计区分性实验 → 定位根因 → 最小修复 → 回归验证。实验必须能排除至少一个候选原因。

### 常见误区

- 先改代码再复现。
- 把相关性当因果。
- 同时尝试多个修复。
- 删除异常保护症状。
- 没有证明回归测试在修复前会失败。

### 完成证据

- 稳定复现记录。
- 事实与假设表。
- 区分性实验结果。
- 最小修复 Diff。
- 修复前失败、修复后通过的回归测试。

### 延伸阅读

[[12-核心执行循环]] · [[14-AI代码评审]] · [[11-AI与TDD]]

## 结语

证据驱动调试把“猜一个修一个”变成可审查的因果推理。延伸阅读：[[14-AI代码评审]]。

---

相关项目文档：[[代码评审一致性清单]]
